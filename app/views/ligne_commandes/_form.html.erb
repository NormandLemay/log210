<%= form_for(@ligne_commande) do |f| %>
  <% if @ligne_commande.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ligne_commande.errors.count, "error") %> prohibited this ligne_commande from being saved:</h2>

      <ul>
      <% @ligne_commande.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
